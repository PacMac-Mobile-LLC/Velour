# Handle client-side routing for React Router
# This file tells the server to serve index.html for all routes
# so React Router can handle the routing

# Redirect non-www to www for consistency
https://vibecodes.space/* https://www.vibecodes.space/:splat 301!

# Redirect all routes to index.html with 200 status
/*    /index.html   200

# Specific redirects for common routes
/dashboard    /index.html   200
/support      /index.html   200
/creators     /index.html   200
/terms        /index.html   200
/privacy      /index.html   200
/auth         /index.html   200
/room/*       /index.html   200
